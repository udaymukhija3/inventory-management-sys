[tool.poetry]
name = "analytics-service"
version = "1.0.0"
description = "ML-powered analytics service for inventory management"
authors = ["Inventory Team"]

[tool.black]
line-length = 100
target-version = ['py311']

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"

